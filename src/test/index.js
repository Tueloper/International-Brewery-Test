import chai from 'chai';

const { expect } = chai;

describe('Initial test', () => {
  it('should be equal to inventory-backend', () => {
    expect('inventory-backend').to.equal('inventory-backend');
  });
});
